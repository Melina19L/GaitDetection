If anaconda is used:

conda env create --name MYENV --file gait.yml

MYENV is gait by default

For pip:

pip install -r requirements.txt

For Movella, follow this to install it:

https://base.movella.com/s/article/Movella-DOT-PC-SDK-Guide?language=en_US